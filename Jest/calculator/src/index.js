const sum = require('./sum');
const subtration = require('./subtration');
const multiplication = require('./multiplication');
const division = require('./division');

module.exports = {
    sum,
    subtration,
    multiplication, 
    division
}